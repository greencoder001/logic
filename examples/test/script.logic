import cli
import random
import gui
import lgptest
import tracking

print('Hello World')

var tCount = 0
var fCount = 0

repeat 100:
    if random.bit() == 1:
        tCount += 1
    else:
        fCount += 1

print(`${fCount}x 0`)
print(`${tCount}x 1`)

var win = new Window()
win.on('ready', -> {
  win.setTitle('Example')
  win.setDir('ltr')
  win.setLang(track().language)
  win.body.set(`
    <h1>Example</h1>
  `)
  win.$('h1').set(win.$('h1').get() + ' <i>(Edited)</i>')
})
